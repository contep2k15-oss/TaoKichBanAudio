# ── Gemini Web automation (Option B) ────────────────────────
playwright>=1.44.0            # sau khi cài: cần Google Chrome, hoặc chạy `playwright install chromium`

# ── Audio / TTS ─────────────────────────────────────────────
edge-tts>=7.0.0               # Microsoft Edge TTS (async). Lỗi 403 → pip install -U edge-tts
pydub>=0.25.1                 # dựng timeline audio trong bộ nhớ
audioop-lts>=0.2.1; python_version >= "3.13"   # pydub cần 'audioop' (bị gỡ khỏi Python 3.13)
numpy>=1.24                   # envelope ducking + mixing

# ── Media ───────────────────────────────────────────────────
opencv-python>=4.8.0          # trích frame + phát hiện cảnh (headless server: dùng opencv-python-headless)
ffmpeg-python>=0.2.0          # dựng lệnh mux FFmpeg

# ── Gemini API (Option A) ───────────────────────────────────
google-genai>=1.0.0           # chỉ cần khi dùng --engine api

# ── Tiện ích ────────────────────────────────────────────────
python-dotenv>=1.0

# Yêu cầu hệ thống (không cài bằng pip): FFmpeg + ffprobe trong PATH
#   Windows: winget install Gyan.FFmpeg     macOS: brew install ffmpeg     Ubuntu: sudo apt install ffmpeg

# ── ElevenLabs (Option TTS thay thế Edge-TTS) ───────────────
httpx>=0.27                   # gọi REST API ElevenLabs (chỉ cần khi --tts-engine elevenlabs)

# ── Giao diện đồ hoạ (GUI) ───────────────────────────────────
streamlit>=1.35              # chạy: streamlit run app_streamlit.py (hoặc double-click Mo_Tool.bat)

# ── App desktop đóng gói (.exe) ──────────────────────────────
pywebview>=5.0             # cửa sổ app riêng (không phải tab trình duyệt) — xem desktop_app.py
pyinstaller>=6.0           # đóng gói thành .exe — xem silent_video_voiceover_tool.spec
pillow>=10.0                # tạo icon app — xem gen_icon.py

# ── Nhập kịch bản từ link YouTube ─────────────────────────────
yt-dlp>=2026.1               # tải video YouTube về máy — xem youtube_source.py
